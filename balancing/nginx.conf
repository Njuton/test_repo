events {}
http {
    upstream backends {
        server app1:8888;
        server app2:8889;
    }
    server {
        listen 80;
        location / {
            proxy_pass http://backends;
        }
    }
}